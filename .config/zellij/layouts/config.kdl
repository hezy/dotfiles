// config.kdl

layout {
    pane command="z ~/.dotfiles && gitui"
    pane split_direction="vertical" {
        pane command="z ~/.dotfiles && find . -maxdepth 1 - type l"
        pane command="z ~/.config && find . -maxdepth 1 - type l"
    }
}
